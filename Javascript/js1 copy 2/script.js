/* funções uteis */

let fruits = ['Uva', 'Maça', 'Banana', 'Abacaxi', 'Carambola'];

/*fruits.sort();*///Muda a ordem do array, ordem alfabetica e provavelmente numerica
//let meKnow = fruits.every((item) => item.length > 3);/* Função para saber se o tamanho da string era maior que 3 */
//let meKnow = fruits.some((item) => item.length < 3 )/* função para saber se algum item satisfaz a condição... */
//join(' como vc quer dividir os itens do array')

console.log(meKnow);
console.log(fruits);





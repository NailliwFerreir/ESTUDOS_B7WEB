tsc diretorioDoArquivo.ts --noEmitOnError //Converte o arquivo para javascript, porém não o fará se estiver com o erro 


dentro da pasta***
tsc nome-do-arquivo -w //significa watchmode 

qualquer alteração sera convertida automaticamente para o arquivo js

tsc --init // irá criar o tsconfig que procurá em todo seu projeto arquivos ts... voce tambem pode unir os dois ultimos comandos

tsc -w  //depois de ter dado o comando tsc init 


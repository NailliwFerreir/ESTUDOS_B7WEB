type PascalCase = any;//tudo começa em maiusculo 
let  camelCase : any = "";//primeira palavra em minusculo 

type NomeCompleto = string;
let nam: NomeCompleto = "Que isso";

/* type User = {
    nome:string,
    idade:number
} */
interface User{
    nome:string,
    idade:number
}